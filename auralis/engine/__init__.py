"""Auralis DSP engine — pure, GUI-agnostic audio processing.

This package has no dependency on the API or frontend. It is usable as a
plain Python library, which keeps it testable and reusable.
"""

__version__ = "0.8.0"
